import { VehicleStatusFormatPipe } from './vehicle-status-format.pipe';

describe('VehicleStatusFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleStatusFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
